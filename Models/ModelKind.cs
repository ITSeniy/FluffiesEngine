namespace FluffiesEngine.Models;

/// <summary>Which builder produces the 3D model.</summary>
public enum ModelKind
{
    /// <summary>The slider-driven procedural fluff (default designer character).</summary>
    Procedural,

    /// <summary>Vark — the bespoke, high-detail latex "Aeroket" character.</summary>
    Vark
}
